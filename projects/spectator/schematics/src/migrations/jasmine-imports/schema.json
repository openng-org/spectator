{
  "$schema": "http://json-schema.org/schema",
  "$id": "SpectatorMigrateJasmineImports",
  "title": "Move Jasmine-flavoured Spectator imports to the @openng/spectator/jasmine entry point",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Limit the migration to a single workspace project. Defaults to the whole workspace.",
      "aliases": ["p"]
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "Limit the migration to a directory. Takes precedence over --project.",
      "visible": false
    }
  },
  "required": []
}
